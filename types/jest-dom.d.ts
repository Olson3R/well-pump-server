// Augments the global `expect` matchers with jest-dom's DOM assertions
// (toBeInTheDocument, toHaveTextContent, etc). Picked up automatically because
// tsconfig includes **/*.ts.
import '@testing-library/jest-dom'
